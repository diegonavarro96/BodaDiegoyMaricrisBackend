# Copy to /etc/wedding-rsvp.env on the server, then `chmod 600 /etc/wedding-rsvp.env`
# Generate a token with: openssl rand -hex 32

PORT=8080
DATA_PATH=/var/www/wedding-rsvp/data/rsvps.json
ADMIN_TOKEN=replace-with-long-random-string
ALLOWED_ORIGIN=https://your-wedding-domain.com
